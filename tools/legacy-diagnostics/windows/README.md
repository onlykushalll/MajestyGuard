# Legacy Windows Diagnostics

Read-only scratch diagnostics preserved from `C:\tmp` because they can help
triage Windows event logs, AppX packaging logs, and companion crashes.

These scripts do not install packages, write certificates, edit registry keys,
enable test signing, or register Credential Providers. They are intentionally
separate from active app code.
